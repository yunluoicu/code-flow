# API Contract

## 字段规则

接口字段使用小驼峰。

## 分层

- `api/`：HTTP 请求封装。
- `types/`：请求/响应类型。
- `composables/`：业务调用逻辑。
- `pages/`：页面使用。

## 要求

1. API client 独立封装。
2. 请求和响应类型显式定义。
3. mock 数据结构接近真实接口。
4. 页面内不要直接拼假数据。
5. 错误信息必须统一处理。
