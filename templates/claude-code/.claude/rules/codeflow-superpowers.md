# codeflow-superpowers

Superpowers 工作流集成约束。将 Superpowers 的标准流程（brainstorming / writing-plans / TDD / review / verification / finishing）绑定到 CodeFlow 工作流中。

必须按顺序使用：brainstorming → writing-plans → executing-plans 或 subagent-driven-development → TDD → review → verification（可选）→ finishing（可选）。
